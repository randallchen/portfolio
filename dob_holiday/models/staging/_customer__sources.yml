sources:
  - name: main
    schema: main
    config:
      freshness:
        warn_after:
          count: 2
          period: day
    tables:
      - name: raw_customers
        loaded_at_field: created_at
        freshness: 
          warn_after: 
            count: 2
            period: day

